vendor/
.idea/
node_modules/
# 046-E1: only Vite build output is ignored; uploads live outside assets/build.
# Keep public/uploads/ (web-accessible) or storage/ (outside docroot) tracked.
/assets/build
nbproject/
files/
.env
/storage/logs/*
!/storage/logs/.gitkeep
.phpunit.result.cache
